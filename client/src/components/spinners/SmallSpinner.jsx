
import Container from "../../assets/containers/SmallSpinner"

const SmallSpinner = ({ size, color, rightSide }) => {
    return (
        <Container size={size} color={color} rightSide={rightSide} />
    )
}

export default SmallSpinner